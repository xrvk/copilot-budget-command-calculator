// Re-exports from the calculator-core package for backward compatibility.
// New code should import from '@copilot-budget/calculator-core' directly.
export type {
  BudgetRecommendations,
  BudgetConstraint,
  CostCenterConstraintInput,
  UserBudgetRecord,
  CCConstraintResult,
  MultiCCConstraintResult,
  ForecastResult,
} from '@copilot-budget/calculator-core'
